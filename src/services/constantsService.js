import { GIPHY_API_KEY, BASE_URL } from "@env";

const constantsService = {
  BASE_URL: BASE_URL,
  GIPHY_API_KEY: GIPHY_API_KEY,
  AGE_RESTRICTION: 16,
  GIFS_FETCH_LIMIT: 20,
  API_GENERAL_ERROR_MSG: "Error occurred while fetching information",
};

export default constantsService;
